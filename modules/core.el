;;; core.el --- Core GNU Emacs configuration module
;;
;;; Commentary:
;;
;;; Code:

(setq make-backup-files nil)
;;(setq native-comp-async-report-warnings-errors 'silent)

(recentf-mode 1)
(which-key-mode 1)

(setq default-input-method "russian-computer")

(provide 'core)

;;; core.el ends here
